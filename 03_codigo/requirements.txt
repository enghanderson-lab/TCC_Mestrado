# PyTorch deve ser instalado separadamente com suporte a CUDA, antes deste arquivo
# (ver README.md para a ordem correta e o arquivo constraints-cuda.txt):
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
numpy
opencv-python
Pillow
pytest
PyYAML
# Busca vetorial — faiss-gpu (se disponível) oferece busca na GPU
# substitua por: pip install faiss-gpu  (requer CUDA)
faiss-cpu
# Profiling — ambos opcionais; degradam graciosamente se ausentes
psutil          # CPU % e RAM (HardwareMonitor)
matplotlib      # gráfico de barras do relatório
# nvidia-ml-py  # GPU utilization % via NVML — descomente se quiser GPU%

# Instalar com: pip install -r requirements.txt -c constraints-cuda.txt
transformers
accelerate
qwen-vl-utils
av
sentence-transformers
sentencepiece
protobuf
bitsandbytes
