# PyTorch deve ser instalado separadamente com suporte a CUDA, antes deste arquivo
# (ver README.md para a ordem correta e o arquivo constraints-cuda.txt):
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
numpy
opencv-python
Pillow
pytest
PyYAML

# Instalar com: pip install -r requirements.txt -c constraints-cuda.txt
transformers
accelerate
qwen-vl-utils
av
sentence-transformers
sentencepiece
protobuf
bitsandbytes
